length of string
s.length();

functions
// arugments --> Black box --> output

pass-by-value
	make copy of argument
	use within function
	returns the same value of variables as the original, not the copy
	
pass-by-reference: input &
	directly reference argument
	use
	
packages
#include <iostream>: gives access to cin cout
#include <math.h>: gives access to sqrt, fabs, floor, ceil, etc.

void returns nothing, can take arguments

